name: Test
on:
  pull_request:
    paths-ignore:
      - demo/**
      - tutorials/**
      - '*.drawio'
      - '*.md'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Test
      run: make ciTest
